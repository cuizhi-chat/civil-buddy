# 给试用的人

Civil Buddy 是土木企业用的**内部讨论 AI 草稿**工作台。不是法定专项方案，不是签认件，不判定可以开工 / 可投标。

成品：GitHub Releases 里的工作台 zip（含 `civil-workbench.exe`）· 源码 MIT，见 [LICENSE](LICENSE)。

## 1. 安装

**方式 A · 下载 exe（不用装 Rust）**

1. 打开 https://github.com/LUOaini1213/civil-buddy/releases ，下载最新 `civil-buddy-workbench-*.zip`。
2. 解压到任意目录（不要解到 `D:\layout`）。
3. 双击 `start-workbench.bat`（zip 内与 exe 同级）。浏览器打开 http://127.0.0.1:8765/

**方式 B · 从源码编**

本机要有 [Rust](https://rustup.rs/) 和 Git。

```powershell
git clone https://github.com/LUOaini1213/civil-buddy.git
cd civil-buddy
copy demo\.env.example demo\.env
cd workbench
cargo build --release --bin civil-workbench
.\target\release\civil-workbench.exe
```

或仓库根：`.\scripts\civil-buddy-desktop.ps1`

## 2. 填 Key（自选模型，不必 DeepSeek）

工作台走 **OpenAI 兼容** `POST /chat/completions`。把 Key 写在 **`demo/.env`**（zip 里与 exe 同级的 `demo\.env`），不要提交、不要贴到 Issue。

任选一种：

```env
# 通用（OpenAI / Groq / Moonshot / SiliconFlow / 本地 vLLM …）
CIVIL_API_KEY=你的key
CIVIL_API_BASE=https://api.openai.com/v1
CIVIL_MODEL=gpt-4o-mini

# 或 OpenAI 风格变量名
OPENAI_API_KEY=你的key
OPENAI_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# 或仍用 DeepSeek
DEEPSEEK_API_KEY=你的key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
```

`CIVIL_*` 优先于 `OPENAI_*`，再才是 `DEEPSEEK_*`。换 Key 后重启工作台。顶栏应显示「已配置 API Key」。401 时核对 Key 与网关地址，**不要**把完整 Key 发出来。

## 3. 选作业根

在同一份 `demo/.env` 里加一行（文件夹必须已存在）：

```env
CIVIL_JOB_ROOT=C:\Users\你\Documents\某工地
```

说「写一份」会读这个夹里的 `.xlsx` / `.docx` / `.csv` / `.txt`，不必再上传。点名已有工作簿时只增改 `CB草稿-*` 表，不改你原来的表。

**禁止**把 `D:\layout` 当作业根。这不是全盘搜索，也不是接管正在打开的 Word/Excel 窗口。

## 4. 点一个岗试「写一份」

1. 左侧点一个岗（例如 **工友白话** / **材料进场**）。只提问不写盘。
2. 高风险岗先勾选：`我明白，将由持证人员签认`。
3. 输入例如：`按某某维修工程写一份班前白话。高度未提供。写一份。`
4. 看对话里的草稿。会话目录会落 `.md`；有表时另有 `.xlsx`。设了作业根会再抄一份到该夹。

缺的数字会标 `[A001]` / `UNSPECIFIED`，不要当现场指令用。源码与规划：仓库 `docs/civil-buddy/GETTING-STARTED.md`。

## 5. 手机上用（可选）

同一 Wi-Fi 下：在 `demo/.env` 加 `CIVIL_HOST=0.0.0.0`，重启工作台，手机打开 `http://<电脑IP>:8765`。顶栏「岗位 / 对话 / 文件」三个按钮拉出侧栏。工作台没有登录，只在自己家里或公司内网开；不要暴露到公网。

成稿中途手机锁屏或断网：已生成的正文保留，落盘的文件在「文件」抽屉能下载；点「停止」可随时中断。
